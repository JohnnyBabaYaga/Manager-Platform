# 指揮中心 v3 — 多部門使用 SOP

## 一句話原則
**程式大家共用，資料各部門一個 private repo。** 採「中央管理」：由平台管理者（Johnny）統一建立各部門 repo 並發一把共用 token；各部門主管只需綁定「自己部門那個 repo」。

---

## 重要觀念：資訊分兩種

| 類型 | 內容 | 存哪 | 換電腦要重做嗎 |
|---|---|---|---|
| **跟著資料走** | 部門資訊、管理者、所有專案/模塊/成員 | 在 `data.json` 裡，隨雲端同步 | ❌ 不用，自動下來 |
| **綁裝置（鑰匙）** | 雲端設定：owner / repo / token | 該電腦瀏覽器本機，不同步 | ✅ 要重填一次 |

> 部門資訊與管理者資訊是「跟著資料走」的，換電腦不用重打；唯一要在新電腦重填的是那把「鑰匙」（owner/repo/token）。token 故意不放進會上雲的資料裡，是安全考量。

---

# A. 給平台管理者（Johnny）— 一次性設定

### A1. 幫每個部門建立 private repo
1. 在自己的 GitHub 帳號 → **＋ → New repository**
2. 一個部門一個，命名如 `cmd-data-display`、`cmd-data-mobility`…
3. **Visibility 一律 Private**
4. Create（空著即可，主管第一次儲存會自動建立 `data.json`）

### A2. 產一把「共用 token」（涵蓋所有部門 repo）
1. GitHub → Settings → Developer settings → **Fine-grained tokens → Generate new token**
2. **Repository access → Only select repositories** → 勾選**全部**部門 repo
3. **Permissions → Contents → Read and write**
4. Expiration 設定到期日（fine-grained 最長一年）→ Generate → **整串複製保存**
5. 把「owner（你的帳號）＋ token ＋ 各部門對應的 repo 名」發給各部門主管

> 之後新增部門：回到 A1 建 repo，並到 token 設定把新 repo 加入勾選即可。
> token 到期前要重產一把，再發給大家更新。

---

# B. 給部門主管 — 第一次設定（約 2 分鐘）

你會從管理者拿到三樣東西：**owner、token、你部門的 repo 名**。

1. 用 **Chrome / Edge** 開平台網址（手機別用 Safari，會卡 token 輸入）
2. 第一次開會跳「**建立你的指揮中心**」精靈 → 填：
   - 部門名稱（會成為左上角標題）
   - 部門代碼（英數，如 `MOB`；左上角標題與未來集團總覽用它區分部門）
   - 識別色（你部門的主題色）
   - 負責主管 / 聯絡 Email
3. 點右上 **☁** → 填雲端同步設定：
   - **owner**：管理者給你的帳號
   - **repo**：你部門那個 repo 名
   - **token**：管理者給你的共用 token（整串貼上）
   - 勾「啟用雲端同步」→ 儲存
4. 完成。之後存檔自動上傳、開啟自動讀取你部門的資料。

---

# C. 換另一台電腦怎麼接？

因為部門資訊與所有資料都「跟著 data.json 走」，換電腦**不用重建部門、不用重打資料**，只要把「鑰匙」重填一次：

1. 開**同一個平台網址**
2. 一開始沒設定會跳「建立你的指揮中心」精靈 → **按「稍後設定」跳過**（別重填，等下從雲端拉就有了）
3. 點右上 **☁** → 填 owner / repo / token（跟原本那台一樣）→ 勾啟用 → 儲存
4. 程式自動從你的 repo 拉 `data.json` → 部門名稱、代碼、識別色、管理者、全部資料**立刻出現**

> 結論：換電腦 = 只重填一次 ☁ 設定，其餘全自動。

---

## 常見狀況
- **Token 401（無效）**：多半是貼錯或頭尾空格 → 整串重貼；或該 token 沒涵蓋你的 repo / Contents 不是 Read and write（這要回報管理者）。
- **看不到資料 / 沒同步**：看右上 ☁ 狀態；確認已勾「啟用雲端同步」、`api.github.com` 連得到。
- **手機**：用 Chrome / Edge，不要用 Safari。
- **改部門名稱/識別色/管理者**：點左上角標題，或右上 i（關於）→ 部門設定。
- **毛玻璃太吃效能（手機/平板卡）**：右上 ⚙️ 調低模糊度。

---

## 關於「集團總覽」（未來）
- 平台已有「🏢 集團總覽」分頁，**目前是規劃中的佔位**，尚未實作彙整。
- 因為採共用 token，那把 token 本來就能讀所有部門的私有 repo → 未來總覽只要「共用 token ＋ 一份部門目錄」逐一讀取彙整即可，**不需要額外的鏡像機制**。
- 第一階段：各部門先各自獨立使用；總覽待第一階段穩定後再開放。
- 這也是第一次設定要填**部門代碼**的原因：未來彙整靠它區分部門。

---

## 安全備註（內部信任環境）
- 共用 token 等於「能讀寫所有部門 repo 的萬用鑰匙」，因此「改不到別人」是**信任制**、非權限強制。
- 請勿把 token 貼到公開場合或截圖外流；外洩時管理者需重產 token 並請大家更新。

---

維護者（平台負責人）：Johnny Yang ｜ johnny.yang@auo.com
